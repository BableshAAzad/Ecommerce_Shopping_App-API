spring:
  application:
    name: Ecommerce_Shopping_App
    #  DataSource configuration
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_shopping_app?createDatabaseIfNotExist=true
    username: root
    password: root
  #      driver-class-name: com.mysql.cj.jdbc.Driver
  #  JPA Configuration
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  # SMTP configuration
  mail:
    host: smtp.gmail.com
    username: aazadbablesh@gmail.com
    password: rlza vblt nfpt wdfz
    properties:
      mail:
        smtp:
          starttls:
            enable: true
            required: true